using UnityEngine;
using System.Collections;
using OpenCvSharp;
using OpenCvSharp.CPlusPlus;

class ThreeDimension : MonoBehaviour
{
    //---------------------------------------------------------
    // 関数名 : getNonBlueNum
    // 機能   : 青以外の情報が格納されている数を取得
    // 引数   : arr/情報
    // 戻り値 : num/青以外の数
    //---------------------------------------------------------
    /*
    public int getNonBlueNum(int[,] arr)
    {
        int num = 0;

        for (int y = 0; y < GlobalVar.CAMERA_HEIGHT / 10; y++)
        {
            for (int x = 0; x < GlobalVar.CAMERA_WIDTH / 10; x++)
            {
                if (arr[y, x] == 1)
                {
                    num++;
                }
            }
        }

        return num;
    }
    */

    //---------------------------------------------------------
    // 関数名 : bondPosStaArr
    // 機能   : 青以外の情報が格納されている数を取得
    // 引数   : x_arr/X情報 y_arr/Y情報
    // 戻り値 : 3d_arr
    //---------------------------------------------------------
    /*
    public int[,,] bondXYArr(int[,] x_arr, int[,] y_arr)
    {
        int[,,] tmp;

        return tmp;
    }
    */
}
